import React from 'react'
import Card from "@material-ui/core/Card";
import Typography from "@material-ui/core/Typography"

const ListCard = ({name}) => {
    return (
        <Card>
            <Typography gutterBottom>{name} awdawdjakwjh</Typography>
        </Card>
    )
}


export default ListCard
